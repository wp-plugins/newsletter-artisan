var configuration = require('./ApplicationConfiguration.json');
var $ = jQuery;


module.exports = function() {

	//check db for changes in settings

	//return default configuration if no changes were made in config

	return configuration;
	
};